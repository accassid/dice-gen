import styled from 'styled-components'

export const UIOptionsDialog = styled.div`
  top: 43px;
  right: 16px;
  width: 38em;
  position: absolute;
  background-color: rgba(28, 28, 28, 0.9);
  box-shadow: 5px 5px 8px 0px rgba(0, 0, 0, 0.68);
  border: 2px solid #434343;
  padding: 0.5em;
  z-index: 5;
  max-width: 95%;
`
