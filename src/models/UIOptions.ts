export type UIOptions = {
  orbitSpeed: number
  showGrid: number
  isUIOptionsPopupVisible: true | false
}

export const DEFAULT_UI_OPTIONS = {
  orbitSpeed: 0.5,
  showGrid: 1,
  isUIOptionsPopupVisible: false,
}
